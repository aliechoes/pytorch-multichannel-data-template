{

	"model": {
		"network": "lenet",
		"device": "cuda",
		"checkpoint_path": null,
		"args": {}
	},
	"loss": {
		"loss_function": "CrossEntropyLoss", 
		"weights": null
	},
	"optimizer": {
		"optimization_method": "adam",
		"optimization_parameters": {
			"lr": 1e-3,
			"weight_decay": 0,
			"amsgrad": true
		}
	},
	"training": {
		"num_epochs": 100,
		"device": "cuda",
		"metrics_of_interest": ["accuracy", "f1_macro", "f1_weighted", "cross_entropy"],
		"call_back": {
			"patience": 30,
			"saving_period": 100,
			"criteria": "f1_macro"
		},
		"verbosity": 2,
		"lr_scheduler": {
			"type": "StepLR",
			"args": {
				"step_size": 50,
				"gamma": 0.1
			}
		}

	},
	"data_loader": {
		"data_dir": "/pstore/home/shetabs1/code/pytorch-multichannel-data-template/sample_data/mnist/",
		"batch_size": 2048,
		"dynamic_range": {
				"lower_bound":0.01,
				"upper_bound":0.99
		},
		"data_map": ["map_zero_one","normalize"],
		"validation_split": 0.25,
		"test_data_dir": null,
		"augmentation": ["random_rotation"],
		"scaling_factor": 255,
		"num_workers": 24

	},
	"tensorboard": {
		"tensorboard_path": "/pstore/home/shetabs1/runs/mnist",
		"add_metrics": true,
		"add_images": true,
		"add_confusion_matrix": true,
		"add_graph": true,
		"add_pr_curve": true,
		"add_hparams": false,
		"add_embedding": true
	}
}